# NREL test fixtures

This directory holds NREL-derived golden data for solar position (SPA) and
clear-sky irradiance validation in `tpt-nrg-solar`.

Current contents:
- (empty — fixtures to be added with Phase 3 solar work)

Expected fixtures:
- `solar-position-spa-golden.json` — zenith/azimuth for known UTC instants.
- `pv-output-derating-golden.json`  — DC output at standard test conditions
  with applied thermal derating and inverter clipping.
